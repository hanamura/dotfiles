# tap
tap homebrew/dupes || true
tap homebrew/versions || true
tap homebrew/homebrew-php || true
tap peco/peco || true

# update
update || true

# upgrade
upgrade || true

# installs
# ========
# git
install git || true
install hub || true
install tig || true

# mercurial
install mercurial || true

# clojure
install leiningen || true

# php & composer
install php55 || true
install composer || true

# go
install go || true

# utilities
install jq || true
install tree || true
install wget || true

# pretty
install figlet || true
install toilet || true
install cowsay || true

# vim
install vim --with-lua || true

# macvim
install macvim --with-lua || true
linkapps || true

# zsh
install zsh || true

# heroku
install heroku-toolbelt || true

# peco
install peco || true

# unix
install gawk || true
install gnu-sed || true

# redis
install redis || true

# mongo
install mongodb || true

# envchain
install https://raw.githubusercontent.com/sorah/envchain/master/brew/envchain.rb || true
